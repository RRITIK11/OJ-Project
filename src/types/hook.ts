/*
Define any utility types that are used throughout your application, such as generic types or type helpers.

Example: types/utils.ts

typescript
Copy code
// types/utils.ts
export type Nullable<T> = T | null;

export type Callback<T> = (data: T) => void;
*/