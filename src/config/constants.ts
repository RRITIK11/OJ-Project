export enum Language {
  Java = "java",
  Cpp = "c++",
  Python = "python",
  JavaScript = "javascript",
}

export enum Success{
    Accepted = "accepted",
    Rejected = "rejected"
}

export enum Difficulty {
    Easy = "easy",
    Medium = "medium",
    Hard = "hard",
}

export enum Verification {
    Pending = "pending",
    Verified = "verified",
    Rejected = "rejected",
    Deleted = "deleted",
  }
  